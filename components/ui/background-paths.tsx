"use client";
import { motion } from "framer-motion";
import { ArrowDown } from "lucide-react";
import { Button } from "./button";
function FloatingPaths({position}:{position:number}){const paths=Array.from({length:28},(_,i)=>`M-${380-i*5*position} -${189+i*6}C-${380-i*5*position} -${189+i*6} -${312-i*5*position} ${216-i*6} ${152-i*5*position} ${343-i*6}C${616-i*5*position} ${470-i*6} ${684-i*5*position} ${875-i*6} ${684-i*5*position} ${875-i*6}`);return <svg className="h-full w-full text-sky-500/40" viewBox="0 0 696 316" fill="none">{paths.map((d,i)=><motion.path key={i} d={d} stroke="currentColor" strokeWidth={.5+i*.03} initial={{pathLength:.3,opacity:.15}} animate={{pathLength:1,opacity:[.12,.52,.12],pathOffset:[0,1,0]}} transition={{duration:20+i%8,repeat:Infinity,ease:"linear"}}/>)}</svg>}
export function BackgroundPaths(){return <section id="inicio" className="relative flex min-h-[550px] items-center justify-center overflow-hidden rounded-[2rem] bg-gradient-to-br from-sky-100 via-white to-blue-100"><div className="absolute inset-0"><FloatingPaths position={1}/><FloatingPaths position={-1}/></div><div className="absolute h-72 w-72 rounded-full bg-sky-300/40 blur-3xl"/><motion.div initial={false} animate={{opacity:1,y:0}} transition={{duration:.7}} className="relative z-10 max-w-3xl px-5 text-center"><p className="mb-5 text-sm font-bold uppercase tracking-[.25em] text-sky-700">BEENIMC textures</p><h1 className="py-1 text-5xl font-black leading-[1.15] tracking-tight text-transparent bg-clip-text bg-gradient-to-r from-sky-500 via-blue-600 to-sky-800 sm:text-7xl">Seu Minecraft, do seu jeito.</h1><p className="mx-auto mt-6 max-w-xl text-lg text-slate-600">Encontre packs de textura, seeds e detalhes que deixam cada mundo com a sua assinatura.</p><Button onClick={()=>document.getElementById("texturas")?.scrollIntoView({behavior:"smooth"})} size="lg" className="mt-8 gap-2 bg-gradient-to-r from-sky-500 to-blue-700 shadow-xl shadow-sky-300/50">Ver texturas <ArrowDown size={18}/></Button></motion.div></section>}
